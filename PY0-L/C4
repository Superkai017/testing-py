import time
def countdown_timer():
    seconds = int(input("enter second:"))
    while seconds >= 0:
        if seconds > 0:
            print(seconds)
        else:
            print("light off")
        time.sleep(0.00001)
        seconds -= 1
countdown_timer()